# Evitar consultas SQL detalladas en los logs públicos.
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# Interpretar correctamente las cabeceras enviadas por el proxy del hosting.
server.forward-headers-strategy=framework

# La cookie de sesión administrativa solo viajará mediante HTTPS.
server.servlet.session.cookie.secure=true
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.same-site=lax

# Permitir que el servidor termine peticiones activas antes de apagarse.
server.shutdown=graceful
spring.lifecycle.timeout-per-shutdown-phase=20s